# Deprecated: hybrid workflow Python scripts

These scripts were part of the Lean+Mathematica+Python hybrid workflow
that was active until 2026-02-22.

The workflow has been split into the separate repository
[`aqei-numerical-validation`](https://github.com/arcticoder/aqei-numerical-validation).

The scripts are archived here for reference only; they are **not** run
by `./run_tests.sh` or any CI pipeline.
